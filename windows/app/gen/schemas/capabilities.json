{"default":{"identifier":"default","description":"Core APIs the window needs. Without this the webview may call the app's own commands but not `listen`, so the phase events that drive the download bar, the recording indicator and the error line never arrive.","local":true,"windows":["main"],"permissions":["core:default"]}}